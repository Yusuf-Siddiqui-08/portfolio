# Version constraints policy:
# - Use compatible-release operator ~= to allow patches and minor updates but block next major.
# - Keep upper bounds implicit via ~= for consistency across all dependencies.
# - Review and update after upstream releases to avoid breakages.
Flask~=2.3.3
Werkzeug~=2.3.8  # ensure compatibility with Flask 2.3.x
requests~=2.31.0
flask-caching~=2.1.0
flask-limiter~=3.5.0
waitress~=2.1.2
psycopg[binary]~=3.1.18
resend~=2.4.0
